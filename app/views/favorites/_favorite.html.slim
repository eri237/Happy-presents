- if item.favorited_by?(current_user)
  = link_to item.favorites.count, item_favorites_path(item), method: :delete, class: "fas fa-heart heart_red"
- else
  = link_to item.favorites.count, item_favorites_path(item), method: :post, class: "fas fa-heart heart_blue"
